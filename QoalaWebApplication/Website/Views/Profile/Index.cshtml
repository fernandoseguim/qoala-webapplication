@{
    ViewBag.Title = "Perfil";
    var currentUser = Session.CurrentUser();

}

@{Html.RenderPartial("../Shared/Logged/_Menu");}
<h2>@ViewBag.Title</h2>
<div>
    <h4>Seu perfil</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>Nome:</dt>
        <dd>
            @currentUser.Name
        </dd>
        <dt>Email:</dt>
        <dd>
            @currentUser.Email
        </dd>
    </dl>
</div>
