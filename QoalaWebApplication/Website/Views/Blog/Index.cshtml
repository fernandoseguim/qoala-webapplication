@{
    ViewBag.Title = "Blog";
}

@{
    if (ViewBag.posts == null)
    {
        <h3> Nada encontrado </h3>
    } else
    {
        foreach (var item in ViewBag.posts)
        {
            <h2>@item.TITLE</h2>
            <div>@item.CONTENT</div>
        }
    }
}